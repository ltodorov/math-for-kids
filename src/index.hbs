<!DOCTYPE html>
<html lang="{{lang}}">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{pageDescription}}">
    <title>{{pageTitle}}</title>
</head>

<body>
    <div class="site">
        <header class="site__header heading" role="banner">
            <img class="heading__image" id="image" src="@images/numbers/0.svg" alt="{{imageText}}" width="62"
                height="80">
            <h1>{{pageTitle}}</h1>
        </header>

        <main class="site__main" role="main">
            <aside class="site__aside" role="complementary">
                <progress class="progress" id="progress" max="20" value="0"></progress>

                <p class="score">
                    {{score}}:
                    <strong class="score__item score__item--ok" id="correct">0</strong>
                    <strong class="score__item score__item--wrong" id="wrong">0</strong>
                </p>
            </aside>

            <article class="content">
                <h2>{{mathTask.title}}</h2>

                <form class="math" id="form">
                    <div class="math__field">
                        <label for="term1" hidden>{{mathTask.term1}}</label>
                        <input type="text" class="math__num" name="term1" id="term-1" disabled>
                    </div>
                    <span class="math__sign" id="operator">+</span>
                    <div class="math__field">
                        <label for="term2" hidden>{{mathTask.term2}}</label>
                        <input type="text" class="math__num" name="term2" id="term-2" disabled>
                    </div>
                    <span class="math__sign math__sign--equal">=</span>
                    <div class="math__field">
                        <label for="answer" hidden>{{mathTask.answer}}</label>
                        <input type="text" class="math__num" name="answer" id="answer" placeholder="?"
                            autocomplete="off" inputmode="numeric">
                    </div>
                    <button type="submit" class="math__btn" id="submit">
                        {{submit}}
                    </button>
                    <p class="math__message" id="message"></p>
                </form>
            </article>
        </main>

        <footer class="site__footer" role="contentinfo">
            <p class="credits">
                {{credits.text}}
                {{#each credits.authors}}
                <a href="{{this.link}}" title="{{this.title}}">{{this.name}}</a>{{#unless @last}},{{/unless}}
                {{/each}}
            </p>
            <p class="copyright">
                {{author}} &copy; <span id="year">2021</span> GPL-3.0-or-later
            </p>
        </footer>
    </div>

</body>

</html>